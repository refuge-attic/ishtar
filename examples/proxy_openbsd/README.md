# ishtar revproxy example

To compile this example :

    $ ../../rebar get-deps compile


You can then start the erlang node with the following command:

    $ ./start.sh


And got to http://localhost:8080, you should see the openbsd website.
